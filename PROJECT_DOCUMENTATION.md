# Ripper Project - 项目文档

> 网页战旗游戏 Ripper-Project 的完整开发文档

**作者**: LEO-CHEP  
**项目愿景**: 献给每个有浪漫梦想的人  
**许可**: 在注明出处的情况下，允许使用该项目进行非商业目的和商业目的的二次创作

---

## 📋 目录

- [项目概述](#项目概述)
- [技术栈](#技术栈)
- [项目架构](#项目架构)
- [核心系统](#核心系统)
- [游戏机制](#游戏机制)
- [开发指南](#开发指南)
- [资源管理](#资源管理)
- [数据结构](#数据结构)
- [功能特性](#功能特性)

---

## 🎮 项目概述

Ripper Project 是一款基于网页的战旗类角色扮演游戏（SRPG/Tactical RPG），灵感来源于龙与地下城（D&D）第四版规则。项目旨在创建一个快速、简洁、可玩性强的战棋游戏Demo。

### 主要特点

- 🎲 **基于D&D 4E规则**: 采用先攻顺序、回合制战斗系统
- 🗺️ **战术地图系统**: 支持格子移动、障碍物、视野遮挡
- ⚔️ **完整战斗系统**: 包含攻击、技能、Buff、状态效果
- 🎭 **剧情系统**: 支持对话、选择、事件触发
- 💾 **存档系统**: 完整的游戏状态保存与读取
- 🎨 **动画系统**: 基于精灵图集的角色和特效动画

### 游戏玩法

玩家可以控制多个角色单位在战术地图上进行回合制战斗，使用各种技能和能力击败敌人，推进剧情发展。游戏包含探索、战斗、对话等多种玩法元素。

---

## 🛠️ 技术栈

### 前端框架
- **Vue 3** (v3.5.13): 采用组合式API（Composition API）
- **TypeScript** (v5.8.0): 用于核心代码开发，提供类型安全
- **Vue Router** (v4.5.0): 路由管理
- **Pinia** (v3.0.1): 状态管理

### 图形引擎
- **PixiJS** (v8.9.2): 2D WebGL 渲染引擎
  - 精灵渲染
  - 纹理管理
  - 容器层级管理
  - 图层系统

### 构建工具
- **Vite** (v6.2.4): 快速的构建工具
- **Vue DevTools**: 开发调试工具

### 数据结构与算法
- **datastructures-algorithms-ts**: 提供高级数据结构支持

### 测试工具
- **Vitest** (v3.2.4): 单元测试框架

---

## 🏗️ 项目架构

### 目录结构

```
ripper-project/
├── public/                     # 静态资源
├── src/
│   ├── assets/                 # 游戏资源文件
│   │   ├── creature/          # 生物精灵图
│   │   ├── door/              # 门素材
│   │   ├── effect/            # 特效素材
│   │   ├── map/               # 地图文件 (.tmj)
│   │   ├── powers/            # 技能配置
│   │   └── ui/                # UI素材
│   ├── class/                 # 基础类定义
│   │   └── Unit.ts           # 单位基类
│   ├── components/            # Vue组件
│   │   ├── GamePannel/       # 游戏主面板
│   │   ├── ActionPannel/     # 行动面板
│   │   ├── CharacterPannel/  # 角色面板
│   │   ├── ActionBar/        # 先攻条
│   │   ├── MessageTipTool/   # 消息提示
│   │   └── PowerTooltip/     # 技能提示
│   ├── core/                  # 核心游戏逻辑
│   │   ├── action/           # 行动模块（移动、攻击）
│   │   ├── ai/               # AI系统
│   │   ├── anim/             # 动画系统
│   │   ├── area/             # 区域系统
│   │   ├── buff/             # Buff系统
│   │   ├── controller/       # 控制器（战斗内/战斗外）
│   │   ├── effect/           # 特效系统
│   │   ├── event/            # 事件系统
│   │   ├── feat/             # 专长系统
│   │   ├── modifier/         # 修正值系统
│   │   ├── power/            # 技能/能力系统
│   │   ├── saver/            # 存档系统
│   │   ├── selector/         # 选择器系统
│   │   ├── stateMachine/     # 状态机
│   │   ├── system/           # 各类游戏系统
│   │   ├── trait/            # 特质系统
│   │   ├── units/            # 单位类（生物、武器、门）
│   │   ├── utils/            # 工具函数
│   │   ├── DiceTryer.ts      # 骰子系统
│   │   ├── MapClass.ts       # 地图类
│   │   └── golbalSetting.ts  # 全局配置
│   ├── drama/                 # 剧情脚本
│   │   ├── drama.ts          # 剧情基类
│   │   └── d1.ts             # 具体剧情
│   ├── stores/                # Pinia状态管理
│   │   ├── characterStore.ts # 角色状态
│   │   ├── initiativeStore.ts# 先攻状态
│   │   ├── message.ts        # 消息状态
│   │   └── talkStateStore.ts # 对话状态
│   ├── utils/                 # 工具函数
│   ├── views/                 # 页面视图
│   │   ├── HomeView.vue      # 主页面（游戏界面）
│   │   └── AboutView.vue     # 关于页面
│   ├── App.vue               # 根组件
│   └── main.ts               # 入口文件
├── dev_log/                   # 开发日志
├── vite.config.ts            # Vite配置
├── tsconfig.json             # TypeScript配置
└── package.json              # 项目配置
```

### 架构设计模式

#### 1. **ECS风格架构** (Entity-Component-System)
- **Entity**: Unit（单位）
- **Component**: Creature（生物属性）、Weapon（武器）、Trait（特质）、Buff等
- **System**: 各种System类（InitiativeSystem、AttackSystem、BuffSystem等）

#### 2. **MVC模式**
- **Model**: Core模块中的各种数据类（Creature、Power、Buff等）
- **View**: Vue组件（GamePannel、ActionPannel等）
- **Controller**: CharacterController、CharacterCombatController等

#### 3. **状态机模式**
- WalkStateMachine: 处理角色移动状态

#### 4. **工厂模式**
- TraitFactory: 创建特质
- 各种Serializer: 序列化和反序列化对象

---

## 🎯 核心系统

### 1. 先攻系统 (InitiativeSystem)

**文件位置**: `src/core/system/InitiativeSystem.ts`

管理战斗中的回合顺序：

- **先攻值计算**: `1d20 + 先攻加值`
- **先攻队列**: 维护所有战斗单位的先攻顺序
- **回合管理**: 
  - `startCombatTurn()`: 开始战斗回合
  - `endTurn()`: 结束当前回合
  - `nextTurn()`: 切换到下一个单位
- **战斗状态**: 
  - 追踪当前回合数
  - 管理战斗开始/结束
  - 检测胜利条件

```typescript
// 核心数据结构
export const InitiativeSheet = [] as InitiativeClass[];
const initiativeCursor = {
  pointAt: null,      // 当前行动单位
  map: null,          // 战斗地图
  inBattle: false,    // 战斗状态
  lastParty: null,    // 上一方阵
  roundCount: 0,      // 回合计数
};
```

### 2. 战斗系统

#### 攻击系统 (AttackSystem)

**文件位置**: `src/core/system/AttackSystem.ts`

- 攻击判定（命中/未命中）
- 伤害计算
- 防御值比较（AC、强韧、反射、意志）
- 暴击判定（通常20命中）

#### 伤害系统 (DamageSystem)

**文件位置**: `src/core/system/DamageSystem.ts`

- 伤害应用
- 抗性计算
- 免疫判定
- 临时生命值处理

#### 机会攻击系统 (OpportunitySystem)

**文件位置**: `src/core/system/OpportunitySystem.ts`

- 监测离开威胁区域的移动
- 触发机会攻击
- 战士特质交互

### 3. 单位系统 (UnitSystem)

**文件位置**: `src/core/system/UnitSystem.ts`

管理游戏中的所有单位：

- **单位创建**: 从配置生成Unit实例
- **生物数据**: Creature类存储完整属性
- **状态管理**: 生命值、位置、状态
- **死亡处理**: 单位死亡时的清理逻辑

```typescript
// Unit核心属性
class Unit {
  uuid: number;              // 唯一标识
  creature: Creature;        // 生物数据
  party: string;             // 阵营（player/enemy）
  initiative: InitiativeClass; // 先攻
  mapPosition: {x, y};       // 地图位置
  state: string;             // 状态（alive/dead）
  controller: any;           // 控制器
  animSpirite: any;         // 动画精灵
}
```

### 4. Buff系统 (BuffSystem)

**文件位置**: `src/core/system/BuffSystem.ts`

管理单位上的增益/减益效果：

- **Buff添加**: `addTo(buff, unit)`
- **Buff移除**: `removeBuff(buff, unit)`
- **修正值更新**: 自动更新单位的属性修正值
- **图标显示**: UI同步
- **时效管理**: 持续时间、触发条件

### 5. 技能系统 (PowerSystem)

**文件位置**: `src/core/system/PowerSystem.ts`

管理单位的技能和能力：

- **技能分类**: 
  - At-will（随意）
  - Encounter（遭遇）
  - Daily（每日）
- **技能使用**: 
  - 范围计算
  - 目标选择
  - 效果应用
- **充能管理**: 技能使用次数

```typescript
class Power {
  name: string;
  type: string;          // at-will/encounter/daily
  action: string;        // 标准/移动/次要/自由
  range: number;
  area?: string;         // 范围（burst/blast等）
  target: string;        // 目标防御值
  attack: string;        // 攻击加值
  hit: string;           // 命中效果
  miss?: string;         // 未命中效果
  effect?: string;       // 额外效果
  keywords?: string[];   // 关键词
}
```

### 6. 修正值系统 (ModifierSystem)

**文件位置**: `src/core/system/ModifierSystem.ts`

管理各种加值和减值：

- **修正值类型**: 增强、力量、士气等
- **堆叠规则**: 同名加值不叠加（通常取最高）
- **自动计算**: 动态计算最终属性值

### 7. 地图系统 (MapClass)

**文件位置**: `src/core/MapClass.ts`

基于Tiled地图编辑器的地图系统：

- **地图格式**: 支持.tmj（Tiled JSON）格式
- **图层系统**:
  - Tile层: 地面、墙壁等
  - Object层: 障碍物、交互物体
- **碰撞检测**: 边界、障碍物
- **视野系统**: 可见性判定
- **门系统**: 可交互的门对象

```typescript
class TiledMap {
  width: number;         // 地图宽度（格子）
  height: number;        // 地图高度（格子）
  tilewidth: number;     // 单格宽度（像素）
  tileheight: number;    // 单格高度（像素）
  layers: Layer[];       // 地图层
  tilesets: Tileset[];   // 图块集
  edges: Edge[];         // 边界/墙壁
  doors: Door[];         // 门
}
```

### 8. 动画系统

**文件位置**: `src/core/anim/`

- **UnitAnimSprite**: 单位动画（行走、攻击、施法）
- **TurnEffectAnim**: 回合特效动画
- **DoorAnimSprite**: 门开关动画
- **精灵图集**: 基于AnimMetaJson的帧动画

### 9. 控制器系统

#### CharacterController
- 角色的基础控制器接口

#### CharacterCombatController（战斗中）
**文件位置**: `src/core/controller/CharacterCombatController.ts`

- 处理战斗中的玩家操作
- 显示移动范围
- 显示攻击范围
- 技能选择和使用

#### CharacterOutCombatController（战斗外）
**文件位置**: `src/core/controller/CharacterOutCombatController.ts`

- 探索模式的角色控制
- 自由移动
- 交互检测
- 对话触发

### 10. 剧情系统 (DramaSystem)

**文件位置**: `src/core/system/DramaSystem.ts` 和 `src/drama/`

- **对话系统**: 
  - `unitSpeak()`: 单位说话
  - `speak()`: 旁白
  - `unitChoose()`: 选择对话
- **事件触发**: 基于变量和标记
- **交互系统**: `addInteraction()` 添加可交互对象
- **剧情变量**: 保存剧情进度

```typescript
class Drama {
  name: string;
  description: string;
  variables: Map<string, any>;  // 剧情变量
  
  play(): void;              // 剧情主逻辑
  loadInit(): void;          // 初始化加载
  CGstart(): void;           // 开始过场
  CGEnd(): void;             // 结束过场
  unitSpeak(): Promise;      // 单位对话
  unitChoose(): Promise;     // 选择对话
  addInteraction(): void;    // 添加交互
}
```

### 11. AI系统

**文件位置**: `src/core/ai/`

- 敌方AI决策
- 行动选择（移动、攻击、使用技能）
- 目标优先级

### 12. 存档系统 (Saver)

**文件位置**: `src/core/saver/Saver.ts`

- **保存游戏状态**: 
  - 所有单位状态
  - 先攻顺序
  - 地图状态
  - 剧情进度
- **读取游戏状态**: 完整恢复游戏
- **序列化器**: 各种Serializer类处理复杂对象

---

## 🎲 游戏机制

### D&D 4E 核心规则

#### 1. 六大属性
- **STR** (力量): 影响近战攻击和力量检定
- **DEX** (敏捷): 影响AC、反射豁免、先攻
- **CON** (体质): 影响HP和强韧豁免
- **INT** (智力): 影响某些技能
- **WIS** (感知): 影响意志豁免和感知技能
- **CHA** (魅力): 影响交涉和社交技能

#### 2. 防御值
- **AC** (护甲等级): 物理攻击防御
- **Fortitude** (强韧): 对抗毒素、疾病
- **Reflex** (反射): 对抗范围效果
- **Will** (意志): 对抗精神效果

#### 3. 攻击判定
```
攻击检定 = 1d20 + 攻击加值
命中 = 攻击检定 >= 目标防御值
```

#### 4. 伤害类型
- 物理伤害
- 元素伤害（火、冰、雷等）
- 力场伤害
- 死灵伤害

#### 5. 动作类型
- **标准动作**: 攻击、施法
- **移动动作**: 移动、站起
- **次要动作**: 饮药、切换武器
- **自由动作**: 说话、丢弃物品
- **即时动作**: 某些反应能力
- **机会动作**: 机会攻击

#### 6. 特殊移动
- **快步 (Shift)**: 移动1格不引发机会攻击
- **冲锋 (Charge)**: 直线移动并攻击，+1命中

### 战斗流程

1. **战斗开始**: 
   - 所有单位投先攻（1d20 + 先攻加值）
   - 按先攻顺序排列

2. **回合开始**:
   - 触发"回合开始"效果
   - 更新Buff持续时间
   - 恢复动作点数

3. **行动阶段**:
   - 标准动作（攻击/技能）
   - 移动动作
   - 次要动作
   - 自由动作

4. **回合结束**:
   - 触发"回合结束"效果
   - 移除过期Buff
   - 切换到下一个单位

5. **战斗结束**:
   - 所有敌人死亡
   - 或所有玩家角色死亡

### 特质系统 (Trait)

**文件位置**: `src/core/trait/`

特质是角色的特殊能力和被动效果：

#### Fighter特质
- **Combat Challenge** (战斗挑战): 
  - 标记敌人
  - 敌人攻击其他目标受惩罚
  - 敌人受到战士攻击时承受额外伤害

#### Cleric特质
- **Healing Word**: 治疗盟友
- **Channel Divinity**: 神圣引导

---

## 💻 开发指南

### 环境要求

- **Node.js**: v22+
- **npm**: v10+
- **现代浏览器**: 支持WebGL 2.0

### 安装与运行

```bash
# 克隆项目
git clone <repository-url>
cd ripper-project

# 安装依赖
npm install

# 开发模式
npm run dev

# 构建生产版本
npm run build

# 预览生产构建
npm run preview

# 运行测试
npm run test

# 类型检查
npm run type-check
```

### 开发流程

#### 1. 添加新的生物

1. 在 `src/assets/creature/` 创建生物文件夹
2. 准备精灵图和动画元数据
3. 在代码中创建Creature配置：

```typescript
const newCreature = new Creature({
  name: "新生物",
  level: 1,
  role: "Soldier",
  hp: 28,
  maxHp: 28,
  ac: 17,
  fortitude: 14,
  reflex: 12,
  will: 12,
  speed: 6,
  initiative: 2,
  abilities: {
    STR: { value: 16, modifier: 3 },
    DEX: { value: 12, modifier: 1 },
    // ... 其他属性
  },
  attacks: [/* 攻击配置 */],
  powers: [/* 技能配置 */],
});
```

#### 2. 添加新的技能

1. 在 `src/assets/powers/` 创建技能JSON
2. 在 `src/core/power/` 中实现技能类：

```typescript
class NewPower extends Power {
  constructor() {
    super({
      name: "技能名称",
      type: "at-will",
      action: "Standard Action",
      range: 10,
      target: "AC",
      // ... 其他配置
    });
  }
  
  async use(caster: Unit, targets: Unit[]): Promise<void> {
    // 技能效果实现
  }
}
```

#### 3. 添加新的Buff

1. 在 `src/core/buff/` 创建Buff类：

```typescript
class NewBuff implements BuffInterface {
  name = "Buff名称";
  description = "Buff描述";
  duration = 3; // 持续回合数
  
  async onApply(unit: Unit): Promise<void> {
    // 应用时效果
  }
  
  async onRemove(unit: Unit): Promise<void> {
    // 移除时效果
  }
  
  async onTurnStart(unit: Unit): Promise<void> {
    // 回合开始时效果
  }
}
```

#### 4. 创建新剧情

1. 在 `src/drama/` 创建剧情文件：

```typescript
class NewDrama extends Drama {
  constructor() {
    super("dramaId", "剧情描述");
  }
  
  play(): void {
    if (!this.getVariable("started")) {
      this.setVariable("started", true);
      this.startSequence();
    }
  }
  
  async startSequence() {
    const { CGstart, unitSpeak, unitChoose, CGEnd } = this;
    
    CGstart();
    await unitSpeak("角色名", "对话内容");
    
    const choice = await unitChoose("角色名", [
      { text: "选项1", value: "opt1" },
      { text: "选项2", value: "opt2" },
    ], "提示文本");
    
    if (choice === "opt1") {
      // 处理选项1
    }
    
    CGEnd();
  }
}
```

### 调试技巧

#### 1. 开启网格显示

在 `GamePannel.vue` 中：

```typescript
const showGridNumbers = ref(true); // 显示格子坐标
```

#### 2. 查看单位状态

```typescript
console.log(unit.creature); // 查看生物属性
console.log(InitiativeSheet); // 查看先攻表
```

#### 3. 测试骰子系统

```typescript
import { diceRoll } from '@/core/DiceTryer';
const result = await diceRoll("1d20+5"); // 投骰子
```

### 代码规范

- **TypeScript**: 所有核心逻辑使用TS编写
- **命名规范**: 
  - 类名：PascalCase
  - 函数/变量：camelCase
  - 常量：UPPER_CASE
- **文件组织**: 按功能模块划分目录
- **注释**: 为复杂逻辑添加注释

---

## 🎨 资源管理

### 精灵图资源

**位置**: `src/assets/creature/`

每个生物需要以下文件：
- `sprite.png`: 精灵图集
- `meta.json`: 动画元数据（帧位置、时长）
- `action.json`: 动作配置（行走、攻击等）

### 地图资源

**位置**: `src/assets/map/`

- 使用 **Tiled Map Editor** 创建
- 导出为 `.tmj` (JSON) 格式
- 支持多图层、对象层

### 特效资源

**位置**: `src/assets/effect/`

- 技能特效动画
- 序列帧图片（APNG/帧序列）
- 提供提取器工具 (`extractor.html`)

### UI资源

**位置**: `src/assets/ui/`

- 按钮、面板等UI元素
- 图标、框架

---

## 📊 数据结构

### Creature（生物）

```typescript
interface CreatureOptions {
  name: string;           // 名称
  level: number;          // 等级
  role: string;           // 角色类型（Soldier/Controller等）
  xp: number;            // 经验值
  size: string;          // 体型（Medium/Large等）
  type: string;          // 类型（Humanoid/Undead等）
  hp: number;            // 当前HP
  maxHp: number;         // 最大HP
  thp?: number;          // 临时HP
  bloodied: number;      // 流血值（通常为最大HP的一半）
  ac: number;            // 护甲等级
  fortitude: number;     // 强韧
  reflex: number;        // 反射
  will: number;          // 意志
  speed: number;         // 移动速度（格子数）
  fly?: number;          // 飞行速度
  initiative: number;    // 先攻加值
  abilities: CreatureAbilities; // 六大属性
  skills?: CreatureSkill[];     // 技能
  attacks?: CreatureAttack[];   // 攻击方式
  powers?: Power[];             // 技能/能力
  traits?: Trait[];             // 特质
  feats?: Trait[];              // 专长
  weapons?: Weapon[];           // 武器
  buffs?: BuffInterface[];      // 当前Buff
  immunities?: string[];        // 免疫
  resistances?: CreatureResistance[]; // 抗性
}
```

### Power（技能）

```typescript
interface PowerOptions {
  name: string;        // 技能名称
  type: string;        // at-will/encounter/daily
  action: string;      // 动作类型
  range: number;       // 范围
  area?: string;       // 作用区域（burst 2, blast 3等）
  target: string;      // 目标防御（AC/Fortitude/Reflex/Will）
  attack: string;      // 攻击加值表达式
  hit: string;         // 命中效果
  miss?: string;       // 未命中效果
  effect?: string;     // 额外效果
  keywords?: string[]; // 关键词（Fire/Thunder/Healing等）
  description?: string;// 描述
}
```

### Buff（增益/减益）

```typescript
interface BuffInterface {
  name: string;           // Buff名称
  description: string;    // 描述
  duration: number;       // 持续回合数（-1表示永久）
  icon?: string;          // 图标
  modifiers?: Modifier[]; // 提供的修正值
  
  // 生命周期钩子
  onApply(unit: Unit): Promise<void>;
  onRemove(unit: Unit): Promise<void>;
  onTurnStart(unit: Unit): Promise<void>;
  onTurnEnd(unit: Unit): Promise<void>;
}
```

### InitiativeClass（先攻）

```typescript
class InitiativeClass {
  value: number;           // 先攻值
  owner: Unit | null;      // 持有者
  ready: boolean;          // 是否准备好
  acted: boolean;          // 是否已行动
}
```

---

## ✨ 功能特性

### 已实现功能

- ✅ 回合制战斗系统
- ✅ 先攻顺序管理
- ✅ 移动和攻击
- ✅ 技能系统（At-will/Encounter/Daily）
- ✅ Buff/Debuff系统
- ✅ 伤害计算和抗性
- ✅ 机会攻击
- ✅ 快步和冲锋
- ✅ 角色动画（行走、攻击、待机）
- ✅ 地图系统（Tiled地图）
- ✅ 碰撞检测
- ✅ 门系统（可开关）
- ✅ 对话系统
- ✅ 剧情触发
- ✅ 存档/读档
- ✅ 战士特质（Combat Challenge）
- ✅ UI系统（先攻条、角色面板、消息提示）
- ✅ 骰子系统
- ✅ 特效动画

### 开发中/计划功能

- 🔄 大体型生物支持
- 📋 更多职业和特质
- 📋 装备系统完善
- 📋 商店系统
- 📋 更多剧情内容
- 📋 AI优化
- 📋 多人联机（未来）

---

## 🎓 学习资源

### D&D 4E规则参考
- [D&D 4th Edition SRD](http://www.wizards.com/)
- 《玩家手册》、《地下城主指南》

### PixiJS文档
- [PixiJS官方文档](https://pixijs.com/guides)
- [PixiJS API参考](https://pixijs.download/release/docs/index.html)

### Vue 3文档
- [Vue 3官方文档](https://cn.vuejs.org/)
- [Pinia状态管理](https://pinia.vuejs.org/zh/)

### Tiled地图编辑器
- [Tiled官网](https://www.mapeditor.org/)
- [Tiled文档](https://doc.mapeditor.org/)

---

## 🙏 致谢

- **Universal-LPC-Spritesheet-Character-Generator**: 提供角色精灵图生成工具
- **PixiJS**: 强大的2D渲染引擎
- **Vue.js**: 优秀的前端框架
- **D&D 4E**: 游戏规则灵感来源

---

## 📝 开发日志

开发日志记录在 `dev_log/` 目录下，按日期组织：

- `2025-7-18.txt`: 战斗阶段控制器、回合聚焦、移动控制器
- `2025-8-13.txt`: 快步、冲锋功能
- `2025-8-16.txt`: 大体型生物（开发中）
- ... 其他日志

---

## 📄 许可证

**在注明出处的情况下，允许使用该项目进行非商业目的和商业目的的二次创作**

请在使用时标注：
- 原作者: LEO-CHEP
- 项目地址: [项目仓库链接]

---

## 📧 联系方式

**维护者**: LEO-CHEP

如有问题或建议，欢迎通过以下方式联系：
- 提交 Issue
- 提交 Pull Request

---

**献给每个有浪漫梦想的人** 🌟
